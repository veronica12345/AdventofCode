switch (matches){
    case 0:
        break;
    case 1:
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 2:
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 3:
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 4:
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 5:
        inputArray.splice(i+5, 0, outputArray[i+5]);
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 6:
        inputArray.splice(i+6, 0, outputArray[i+6]);
        inputArray.splice(i+5, 0, outputArray[i+5]);
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 7:
        inputArray.splice(i+7, 0, outputArray[i+7]);
        inputArray.splice(i+6, 0, outputArray[i+6]);
        inputArray.splice(i+5, 0, outputArray[i+5]);
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 8:
        inputArray.splice(i+8, 0, outputArray[i+8]);
        inputArray.splice(i+7, 0, outputArray[i+7]);
        inputArray.splice(i+6, 0, outputArray[i+6]);
        inputArray.splice(i+5, 0, outputArray[i+5]);
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 9:
        inputArray.splice(i+9, 0, outputArray[i+9]);
        inputArray.splice(i+8, 0, outputArray[i+8]);
        inputArray.splice(i+7, 0, outputArray[i+7]);
        inputArray.splice(i+6, 0, outputArray[i+6]);
        inputArray.splice(i+5, 0, outputArray[i+5]);
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
    case 10:
        inputArray.splice(i+10, 0, outputArray[i+10]);
        inputArray.splice(i+9, 0, outputArray[i+9]);
        inputArray.splice(i+8, 0, outputArray[i+8]);
        inputArray.splice(i+7, 0, outputArray[i+7]);
        inputArray.splice(i+6, 0, outputArray[i+6]);
        inputArray.splice(i+5, 0, outputArray[i+5]);
        inputArray.splice(i+4, 0, outputArray[i+4]);
        inputArray.splice(i+3, 0, outputArray[i+3]);
        inputArray.splice(i+2, 0, outputArray[i+2]);
        inputArray.splice(i+1, 0, outputArray[i+1]);
        break;
} 